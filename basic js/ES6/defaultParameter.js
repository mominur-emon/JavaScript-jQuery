"use strict"; //use for strict mode

function message(text = "This is default parameter") {
  console.log(`${text}`);
}

message(); //no test pass so showing default test
message("I love Bangladesh");
